# .env (example — fill values)
PAIRS=EURUSD,GBPUSD,USDJPY    # comma-separated list of currency pairs
EMAIL_DRYRUN=False            # True = only print, False = actually send
EMAIL_FROM=youremail@gmail.com
EMAIL_TO=target@example.com

# SMTP settings for Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587                 # 587 for STARTTLS, 465 for SSL
SMTP_USER=youremail@gmail.com
SMTP_PASS=your_app_password   # App Password (preferred) or account password (not recommended)

# Optional: logging/traces
TRACE_DIR=src/data/traces
